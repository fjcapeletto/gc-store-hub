# ---------------------------------------------------------------------------
# Gabriel Capeletto Store — Hub client (.NET)
# Public repository. Nothing secret is allowed to land here, ever.
# ---------------------------------------------------------------------------

# Contract negotiation vaivém — working files between the hub & server sessions.
# NEVER committed: only ratified normative interfaces live in the repo (see contract/README.md).
# These stay local for the round, then get deleted. Normative docs have plain names.
contract/*.draft.md
contract/*.review.md
contract/*.response.md
contract/*-response.md
contract/*.ratify.md
contract/*-ratify.md
contract/*.request.md
contract/*.update.md
contract/*-handoff.md
contract/*.hub-*.md
contract/*.backend-*.md

# Internal build/rollout recipes — kept local, not part of the public boundary
# contract. Sanitize before ever publishing one of these.
contract/web-account-rollout.md

# Build output
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/
artifacts/
publish/
[Rr]eleases/

# Visual Studio / Rider / VS Code
.vs/
.vscode/
.idea/
*.user
*.suo
*.userprefs
*.rsuser
*.sln.docstates
*_i.c
*_p.c

# .NET
project.lock.json
project.fragment.lock.json
*.nupkg
*.snupkg
.packages/
packages/
!packages/build/
msbuild.log
msbuild.err
msbuild.wrn

# Test / coverage
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
coverage*.info
*.coverage
*.coveragexml

# ---------------------------------------------------------------------------
# SECRETS — must never be committed (public repo, history is forever)
# Real config comes from user-secrets (dev) and env / secret manager (prod).
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
appsettings.*.json
!appsettings.example.json
!appsettings.Development.example.json
secrets.json
*.pfx
*.snk
*.p12
*.key
*.pem

# OS noise
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store
